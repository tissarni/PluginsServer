version: "3.9"

volumes:
  plugins:
    

services:
  plugins:
    container_name: plugins_container
    image: plugins
    build: 
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - "3100:3100"
    