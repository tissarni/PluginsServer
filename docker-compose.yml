version: "3.9"

volumes:
  plugins:
    

services:
  plugins:
    container_name: plugins_container
    image: plugins
    privileged: true
    build: 
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - "3100:3100"
    